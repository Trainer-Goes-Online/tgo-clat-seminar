/* Razorpay checkout removed: the seminar is FREE.
   This file is no longer used (checkout/page.tsx redirects to /register).
   Kept only as a harmless stub so no Razorpay code ships. */
export default function CheckoutClient() {
  return null;
}
